strip_tags() removes HTML and PHP tags from the input, so only the content between the opening and closing tags are displayed, and not the tags themselves.

htmlspecialchars() converts special characters from the input into HTML entities, so exactly what is typed is what is displayed, but not executed.